{
  "name": "maxq-dev",
  "version": "0.1.0",
  "description": "CLI tooling for MAX-Q00",
  "private": true,
  "type": "module",
  "bin": {
    "maxq-dev": "dist/index.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "npm run build && node dist/index.js",
    "test": "npm run build && node --test test/*.test.js"
  },
  "engines": {
    "node": ">=22.12.0"
  },
  "keywords": [
    "cli",
    "maxq"
  ],
  "license": "UNLICENSED",
  "dependencies": {
    "commander": "^15.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.20.3",
    "typescript": "^7.0.2"
  }
}
